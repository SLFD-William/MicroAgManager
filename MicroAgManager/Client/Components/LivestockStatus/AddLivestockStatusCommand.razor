@using Domain.Constants;
@using Domain.Models;
@inherits DataComponent

@if (editContext is not null)
{
    <CommandButton editContext="editContext"
                   FreshenData="FreshenData"
                   OnValidSubmit="OnSubmit"
                   Cancel="Cancel"
                   Command="@Command"
                   showUpdateButton="true" @ref=_commandButton>
        <DataContent>
            <CascadingValue Value="livestockStatus">
                <LivestockStatusFields></LivestockStatusFields>
            </CascadingValue>
        </DataContent>
    </CommandButton>
}